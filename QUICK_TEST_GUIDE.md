# 📱 Quick Test Guide - ทดลองเข้าจากโทรศัพท์

## 🚀 วิธีที่ 1: ใช้ Local Network (ง่ายที่สุด - แนะนำ)

### ขั้นตอน:

**1. เช็ค IP Address ของคอมพิวเตอร์:**
```powershell
# Windows
ipconfig

# หา IPv4 Address (เช่น 192.168.1.100)
```

**2. รัน Development Server:**
```bash
npm run dev
```

**3. เปิด Next.js ให้ listen ทุก network:**
```bash
# Next.js จะ listen ที่ 0.0.0.0 โดยอัตโนมัติ
# แต่ต้องตรวจสอบว่าพอร์ต 3000 เปิดอยู่
```

**4. เข้าจากโทรศัพท์:**
```
http://192.168.1.100:3000
```
(แทนที่ `192.168.1.100` ด้วย IP address จริงของคุณ)

---

### ⚠️ ถ้าเข้าไม่ได้:

**A. ตรวจสอบ Firewall:**
```powershell
# Windows - เปิด Port 3000 ใน Firewall
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

**B. ตรวจสอบว่า Next.js listen ที่ 0.0.0.0:**
```bash
# Next.js 16 จะ listen ที่ 0.0.0.0 โดยอัตโนมัติแล้ว
# ถ้ายังไม่ได้ ลอง:
npm run dev -- -H 0.0.0.0
```

---

## 🌐 วิธีที่ 2: ใช้ ngrok (สำหรับเข้าจากอินเทอร์เน็ต - ทดสอบจากที่ไหนก็ได้)

### ขั้นตอน:

**1. ติดตั้ง ngrok:**
```bash
# Download จาก https://ngrok.com/download
# หรือใช้ npm
npm install -g ngrok
```

**2. Sign up ที่ ngrok.com (ฟรี)**

**3. รับ Auth Token:**
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

**4. รัน Next.js:**
```bash
npm run dev
```

**5. เปิด ngrok tunnel:**
```bash
ngrok http 3000
```

**6. Copy URL จาก ngrok:**
```
Forwarding  https://xxxx-xxxx-xxxx.ngrok-free.app -> http://localhost:3000
```

**7. เข้าจากโทรศัพท์:**
```
https://xxxx-xxxx-xxxx.ngrok-free.app
```

**⚠️ หมายเหตุ:** 
- URL ฟรีจะเปลี่ยนทุกครั้งที่ restart
- มี rate limiting (สำหรับฟรี tier)
- ใช้สำหรับทดสอบเท่านั้น

---

## 🚀 วิธีที่ 3: Deploy ไป Vercel (ฟรี - แนะนำสำหรับ deploy จริง)

### ขั้นตอน:

**1. Push code ไป GitHub (ถ้ายังไม่ได้):**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

**2. ไปที่ [Vercel](https://vercel.com) และ Sign in ด้วย GitHub**

**3. Import Project:**
- คลิก "New Project"
- เลือก GitHub repository
- Vercel จะ detect Next.js อัตโนมัติ

**4. ตั้งค่า Environment Variables:**
```env
DATABASE_URL="file:./prisma/dev.db"  # สำหรับ SQLite (ทดสอบ)
NODE_ENV="production"
```

**5. Deploy!**
- คลิก "Deploy"
- รอสักครู่ (ประมาณ 2-5 นาที)
- ได้ URL: `https://your-project.vercel.app`

**6. เข้าจากโทรศัพท์:**
```
https://your-project.vercel.app
```

**⚠️ หมายเหตุ:** 
- SQLite ไม่เหมาะสำหรับ production (ไฟล์จะ reset ทุกครั้งที่ deploy ใหม่)
- สำหรับทดสอบใช้ได้ แต่ถ้า deploy จริงต้องใช้ PostgreSQL

---

## 📊 เปรียบเทียบวิธีต่างๆ

| วิธี | ความยาก | ใช้เวลา | ฟรี? | เหมาะสำหรับ |
|------|---------|---------|------|-------------|
| **Local Network** | ⭐ ง่าย | 1 นาที | ✅ ฟรี | ทดสอบในบ้าน/ออฟฟิศ |
| **ngrok** | ⭐⭐ ปานกลาง | 5 นาที | ✅ ฟรี (มี limit) | ทดสอบจากที่ไหนก็ได้ |
| **Vercel** | ⭐⭐ ปานกลาง | 10 นาที | ✅ ฟรี | Deploy จริง |

---

## ✅ แนะนำสำหรับคุณ

**ถ้าแค่ทดสอบด่วน:**
- ใช้ **Local Network** (วิธีที่ 1) - ง่ายที่สุด
- หรือ **ngrok** (วิธีที่ 2) - ถ้าต้องการเข้าจากที่ไหนก็ได้

**ถ้าต้องการ deploy จริง:**
- ใช้ **Vercel** (วิธีที่ 3) - ฟรีและง่าย

---

## 🎯 Quick Start (Local Network)

```bash
# 1. เช็ค IP Address
ipconfig

# 2. รัน Development Server
npm run dev

# 3. เข้าจากโทรศัพท์
# http://YOUR_IP_ADDRESS:3000
```

**ตัวอย่าง:**
```
คอมพิวเตอร์ IP: 192.168.1.100
เข้าจากโทรศัพท์: http://192.168.1.100:3000
```

---

## ⚠️ Troubleshooting

### ถ้าเข้าไม่ได้จากโทรศัพท์:

**1. ตรวจสอบว่าโทรศัพท์อยู่ network เดียวกัน:**
- WiFi เดียวกัน
- หรือ Mobile Hotspot จากคอมพิวเตอร์

**2. ตรวจสอบ Firewall:**
```powershell
# เปิด Port 3000
New-NetFirewallRule -DisplayName "Next.js" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

**3. ตรวจสอบว่า Next.js ทำงานอยู่:**
```bash
# ควรเห็น:
# ✓ Ready in XXXms
# - Local: http://localhost:3000
# - Network: http://192.168.1.100:3000
```

---

## 🎉 เสร็จแล้ว!

เลือกวิธีที่คุณต้องการลองได้เลยครับ! 

**แนะนำ:** เริ่มจาก **Local Network** (วิธีที่ 1) เพราะง่ายที่สุด!

