# 📱 คู่มือทดสอบเข้าจากโทรศัพท์

## 🚀 วิธีที่ 1: Local Network (ง่ายที่สุด - แนะนำ)

### ขั้นตอน:

**1. รัน Development Server:**
```bash
npm run dev
```

หรือถ้าต้องการระบุ host แน่ชัด:
```bash
npm run dev:network
```

**2. ดู IP Address:**
```
IP Address ของคุณ: 192.168.31.165
```

**3. เข้าจากโทรศัพท์ (WiFi เดียวกัน):**
```
http://192.168.31.165:3000
```

**⚠️ หมายเหตุ:**
- โทรศัพท์ต้องอยู่ WiFi เดียวกันกับคอมพิวเตอร์
- ถ้าเข้าไม่ได้ ตรวจสอบ Firewall (ดูด้านล่าง)

---

## 🌐 วิธีที่ 2: ใช้ ngrok (เข้าจากที่ไหนก็ได้)

### ขั้นตอน:

**1. ติดตั้ง ngrok:**
```bash
# Download จาก https://ngrok.com/download
# หรือใช้ Chocolatey (Windows)
choco install ngrok
```

**2. Sign up ที่ [ngrok.com](https://ngrok.com) (ฟรี)**

**3. รับ Auth Token:**
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

**4. รัน Next.js:**
```bash
npm run dev
```

**5. เปิด ngrok tunnel (terminal ใหม่):**
```bash
ngrok http 3000
```

**6. Copy URL จาก ngrok:**
```
Forwarding  https://xxxx-xxxx-xxxx.ngrok-free.app -> http://localhost:3000
```

**7. เข้าจากโทรศัพท์:**
```
https://xxxx-xxxx-xxxx.ngrok-free.app
```

**⚠️ หมายเหตุ:**
- URL ฟรีจะเปลี่ยนทุกครั้งที่ restart
- มี rate limiting (สำหรับฟรี tier)
- เหมาะสำหรับทดสอบเท่านั้น

---

## 🔥 ถ้าเข้าไม่ได้ (Firewall)

### Windows Firewall:

```powershell
# เปิด Port 3000 ใน Firewall (Run as Administrator)
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

หรือใช้ GUI:
1. ไปที่ Windows Defender Firewall
2. Advanced settings
3. Inbound Rules → New Rule
4. Port → TCP → Specific local ports: 3000
5. Allow connection
6. Next → Finish

---

## ✅ Quick Start (วิธีที่ง่ายที่สุด)

```bash
# 1. รัน Development Server
npm run dev

# 2. เข้าจากโทรศัพท์
# http://192.168.31.165:3000
```

**ตัวอย่าง:**
```
คอมพิวเตอร์: 192.168.31.165
เข้าจากโทรศัพท์: http://192.168.31.165:3000
```

---

## 📊 เปรียบเทียบวิธีต่างๆ

| วิธี | ความยาก | ใช้เวลา | ฟรี? | เหมาะสำหรับ |
|------|---------|---------|------|-------------|
| **Local Network** | ⭐ ง่าย | 1 นาที | ✅ ฟรี | ทดสอบในบ้าน/ออฟฟิศ |
| **ngrok** | ⭐⭐ ปานกลาง | 5 นาที | ✅ ฟรี (มี limit) | ทดสอบจากที่ไหนก็ได้ |

---

## 🎯 แนะนำสำหรับคุณ

**ถ้าแค่ทดสอบด่วน:**
- ใช้ **Local Network** (วิธีที่ 1) - ง่ายที่สุด
- IP Address: `192.168.31.165`
- URL: `http://192.168.31.165:3000`

**ถ้าต้องการเข้าจากที่ไหนก็ได้:**
- ใช้ **ngrok** (วิธีที่ 2)

---

## ⚠️ Troubleshooting

### ถ้าเข้าไม่ได้จากโทรศัพท์:

**1. ตรวจสอบว่าโทรศัพท์อยู่ network เดียวกัน:**
- WiFi เดียวกัน
- หรือ Mobile Hotspot จากคอมพิวเตอร์

**2. ตรวจสอบ Firewall:**
```powershell
# เปิด Port 3000
New-NetFirewallRule -DisplayName "Next.js" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

**3. ตรวจสอบว่า Next.js ทำงานอยู่:**
```bash
# ควรเห็น:
# ✓ Ready in XXXms
# - Local: http://localhost:3000
# - Network: http://192.168.31.165:3000
```

**4. ตรวจสอบ IP Address:**
```powershell
ipconfig
# หา "IPv4 Address" (เช่น 192.168.31.165)
```

---

## 🎉 เสร็จแล้ว!

**ลองใช้วิธี Local Network ก่อนครับ:**
```bash
npm run dev
# แล้วเข้าจากโทรศัพท์: http://192.168.31.165:3000
```

ง่ายที่สุด! 🚀

