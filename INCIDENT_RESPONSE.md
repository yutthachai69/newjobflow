# 🚨 Security Incident Response Plan

## เมื่อถูกแฮ็ก - ขั้นตอนการจัดการ

### 1. การตรวจจับ (Detection)

#### สัญญาณที่บ่งชี้ว่าถูกแฮ็ก:
- ✅ **Rate Limiting**: ระบบจะล็อคบัญชีอัตโนมัติเมื่อมี failed login attempts เกิน 5 ครั้ง
- ✅ **Security Logging**: ทุก security event ถูกบันทึกใน console (ใน production ควรส่งไป SIEM)
- ✅ **Suspicious Activity**: การเข้าถึงข้อมูลผิดปกติ, การแก้ไขข้อมูลโดยไม่ได้รับอนุญาต

#### วิธีตรวจสอบ:
1. ตรวจสอบ Security Logs ใน console
2. ตรวจสอบ failed login attempts
3. ตรวจสอบการเปลี่ยนแปลงข้อมูลที่ผิดปกติ

---

### 2. การตอบสนองทันที (Immediate Response)

#### ขั้นตอนที่ต้องทำทันที:

1. **ล็อคบัญชีที่ถูกแฮ็ก**
   ```bash
   # ใช้ API endpoint (ADMIN only)
   POST /api/security/lock-account
   {
     "userId": "user_id_here",
     "reason": "Suspected security breach"
   }
   ```

2. **เปลี่ยนรหัสผ่านทันที**
   - เปลี่ยนรหัสผ่านของบัญชีที่ถูกแฮ็ก
   - เปลี่ยนรหัสผ่านของ ADMIN account
   - ใช้รหัสผ่านที่แข็งแกร่ง

3. **ตรวจสอบ Session**
   - ลบ session ทั้งหมด (clear all cookies)
   - บังคับให้ผู้ใช้ login ใหม่

4. **รายงานเหตุการณ์**
   ```bash
   POST /api/security/incident
   {
     "incidentType": "UNAUTHORIZED_ACCESS",
     "description": "Account compromised",
     "severity": "HIGH",
     "affectedUsers": ["user_id_1", "user_id_2"]
   }
   ```

---

### 3. การวิเคราะห์ (Analysis)

#### ตรวจสอบ:
1. **Log Files**
   - ตรวจสอบ security logs
   - ตรวจสอบ failed login attempts
   - ตรวจสอบการเข้าถึงข้อมูล

2. **Database**
   - ตรวจสอบการเปลี่ยนแปลงข้อมูล
   - ตรวจสอบ user activity
   - ตรวจสอบ work orders, assets ที่ถูกแก้ไข

3. **Network**
   - ตรวจสอบ IP addresses ที่ผิดปกติ
   - ตรวจสอบ geographic location

---

### 4. การกู้คืน (Recovery)

#### ขั้นตอนการกู้คืน:

1. **ลบ Session ที่ถูกแฮ็ก**
   - Clear all sessions
   - Force re-login

2. **เปลี่ยนรหัสผ่าน**
   - เปลี่ยนรหัสผ่านของบัญชีที่ถูกแฮ็ก
   - เปลี่ยนรหัสผ่านของบัญชีอื่นๆ ที่อาจได้รับผลกระทบ

3. **ตรวจสอบข้อมูล**
   - ตรวจสอบว่าข้อมูลถูกแก้ไขหรือไม่
   - Restore จาก backup ถ้าจำเป็น

4. **แจ้งผู้ใช้**
   - แจ้งผู้ใช้ที่ได้รับผลกระทบ
   - แนะนำให้เปลี่ยนรหัสผ่าน

---

### 5. การป้องกันในอนาคต (Prevention)

#### มาตรการที่ควรเพิ่ม:

1. **Two-Factor Authentication (2FA)**
   - เพิ่ม 2FA สำหรับ ADMIN และ sensitive operations

2. **Security Monitoring**
   - ใช้ SIEM system (Security Information and Event Management)
   - Real-time alerts

3. **Regular Security Audits**
   - ตรวจสอบ security logs เป็นประจำ
   - ตรวจสอบ user permissions
   - ตรวจสอบ access patterns

4. **Backup & Recovery**
   - Backup ข้อมูลเป็นประจำ
   - Test recovery procedures

5. **Security Training**
   - อบรมผู้ใช้เกี่ยวกับ security best practices
   - Phishing awareness

---

## Security Features ที่มีอยู่แล้ว

### ✅ Rate Limiting
- ล็อคบัญชีอัตโนมัติเมื่อมี failed login attempts เกิน 5 ครั้ง
- Lockout duration: 15 นาที

### ✅ Security Logging
- บันทึกทุก security event
- Failed login attempts
- Account lockouts
- Successful logins

### ✅ Security Headers
- X-Content-Type-Options: nosniff
- X-Frame-Options: DENY
- X-XSS-Protection: 1; mode=block
- Content-Security-Policy
- Strict-Transport-Security (production)

### ✅ Password Security
- Bcrypt hashing (salt rounds = 10)
- Password strength validation
- Weak password detection

### ✅ Authorization
- Role-based access control (RBAC)
- Ownership checks
- Admin-only operations

---

## Emergency Contacts

- **Security Team**: [เพิ่มข้อมูลติดต่อ]
- **System Administrator**: [เพิ่มข้อมูลติดต่อ]
- **Incident Response Team**: [เพิ่มข้อมูลติดต่อ]

---

## Checklist เมื่อถูกแฮ็ก

- [ ] ตรวจจับและยืนยันการถูกแฮ็ก
- [ ] ล็อคบัญชีที่ถูกแฮ็กทันที
- [ ] เปลี่ยนรหัสผ่านของบัญชีที่ถูกแฮ็ก
- [ ] Clear all sessions
- [ ] รายงานเหตุการณ์ผ่าน API
- [ ] วิเคราะห์ scope ของการถูกแฮ็ก
- [ ] ตรวจสอบข้อมูลที่ถูกแก้ไข
- [ ] Restore จาก backup ถ้าจำเป็น
- [ ] แจ้งผู้ใช้ที่ได้รับผลกระทบ
- [ ] Document incident
- [ ] Review และปรับปรุง security measures

